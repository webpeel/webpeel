# WebPeel — The Web Data Platform for AI Agents

> Fetch, search, crawl, extract, monitor, screenshot, and research any URL. One API call. 65-98% token savings.

## Quick Decision Tree

Choose the right approach:
- Static page → `webpeel <url>` (default, fastest)
- JavaScript SPA (React, Vue, Next.js) → `webpeel <url> --render`
- Bot-protected site (Cloudflare) → `webpeel <url> --stealth`
- Infinite scroll / lazy content → `webpeel <url> --render --action 'scroll:bottom' --action 'wait:2000'`
- Interact (click, type) → `webpeel <url> --render --action 'click:.btn' --action 'wait:1000'`
- Screenshot → `webpeel screenshot <url>`
- YouTube transcript → `webpeel <youtube-url>`
- PDF → `webpeel <pdf-url>`
- Structured data → `webpeel <url> --schema product --json`
- Web search → `webpeel search "query"`
- Monitor for changes → `webpeel watch <url>`

## Capabilities

- **Fetch**: Extract clean markdown from any URL. Auto-handles JS rendering, Cloudflare, CAPTCHAs.
- **Search**: Web search with structured results. Smart search detects intent (restaurants, products, flights, hotels).
- **Crawl**: Crawl sites up to 100 pages with dedup and rate limiting.
- **Extract**: JSON schema extraction. Built-in schemas: product, article, recipe, job, event, contact, review, listing.
- **Monitor**: Track pages for changes with diffs and CSS selector targeting.
- **Screenshot**: Full-page screenshots with device emulation. Visual diff comparison.
- **Research**: Autonomous research agent — plans queries, fetches sources, synthesizes with citations.
- **YouTube**: Full transcripts with timestamps, 50+ languages.
- **PDF**: Text extraction from PDFs.
- **Act**: Browser automation — click, fill forms, scroll, interact, then extract.

## Browser Actions (--action flag)

For dynamic pages. Auto-enables browser rendering. Chain multiple:

- `scroll:bottom` — scroll to bottom (infinite scroll)
- `scroll:down:500` — scroll down 500px
- `wait:2000` — wait 2 seconds
- `click:.selector` — click an element
- `type:#input:text` — type into a field
- `waitFor:.selector` — wait for element to appear

Example: `webpeel <url> --render --action 'scroll:bottom' --action 'wait:2000'`

## API Endpoints

- `GET /v1/fetch?url={url}` — Fetch content as markdown
- `POST /v1/fetch` — Fetch with options (render, stealth, actions, schema)
- `GET /v1/search?q={query}` — Web search
- `POST /v1/search/smart` — AI-powered smart search
- `POST /v1/screenshot` — Screenshot (mobile/tablet/desktop)
- `POST /v1/crawl` — Crawl a site
- `POST /v1/extract` — Structured data extraction
- `GET /v1/feed?url={url}` — RSS/Atom feeds

## MCP Server (Claude, Cursor, VS Code)

```json
{
  "mcpServers": {
    "webpeel": {
      "command": "npx",
      "args": ["-y", "webpeel", "mcp"]
    }
  }
}
```

### MCP Tools
- **webpeel** — Natural language web tool. Describe what you want.
- **webpeel_read** — Fetch URL as markdown. Use render=true for SPAs, actions=[] for interaction.
- **webpeel_see** — Screenshots with mobile/tablet/desktop viewports.
- **webpeel_find** — Web search or sitemap discovery.
- **webpeel_extract** — JSON extraction with schema or field names.
- **webpeel_monitor** — Track pages for changes over time.
- **webpeel_act** — Browser automation (click, type, scroll).

## 55+ Domain Extractors

Instant structured data without browser rendering:
Amazon, Reddit, GitHub, YouTube, Wikipedia, ESPN, Polymarket, Kalshi, TradingView,
Hacker News, NPM, PyPI, Stack Overflow, Medium, Substack, IMDb, Yelp, Zillow,
eBay, Walmart, Best Buy, and 30+ more.

## Token Efficiency

65-98% fewer tokens vs raw HTML:
- BBC News: 98% (100K → 2,050 tokens)
- Wikipedia: 86%
- GitHub: 80%+

## Authentication (Login-Protected Pages)

WebPeel auto-detects auth walls. When detected, it returns `authRequired: true` and instructions.

To access login-protected content:
1. `webpeel profile create <site>` — opens browser, log in normally, press Ctrl+C
2. `webpeel "<url>" --profile <site>` — fetches with saved cookies

Profiles are saved in `~/.webpeel/profiles/` and reusable across sessions.

## Troubleshooting

- **Very little content?** → Site is likely a JavaScript SPA. Add `--render`.
- **Still blocked?** → Add `--stealth` for anti-bot bypass.
- **SPA not loading?** → Add `--action 'wait:3000'` for slow SPAs.
- **Screenshot fails?** → Run: `npx playwright install chromium`
- **Wrong content for sub-pages?** → Content may be client-side routed. Use `--render`.
- **Auth wall detected?** → Run `webpeel profile create <site>`, log in, then use `--profile <site>`.

## Quick Start

```bash
npx webpeel "https://example.com"                     # Basic fetch
npx webpeel "https://spa-site.com" --render            # JavaScript SPA
npx webpeel "https://site.com" --stealth               # Bot-protected
npx webpeel screenshot "https://example.com"           # Screenshot
npx webpeel search "best AI frameworks"                # Search
npx webpeel "https://amazon.com/dp/..." --schema product --json  # Structured
npx webpeel guide                                      # Full AI usage guide
```

## Pricing

Free: 2,000 requests/month (no credit card) | Starter: $9/mo (10K) | Pro: $49/mo (100K) | Enterprise: Custom

## Links

- Docs: https://webpeel.dev/docs
- API Reference: https://webpeel.dev/docs/api-reference
- Dashboard: https://app.webpeel.dev
- GitHub: https://github.com/webpeel/webpeel
- npm: https://www.npmjs.com/package/webpeel
