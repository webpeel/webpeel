{
  "name": "webpeel-cf-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for WebPeel anti-bot bypass",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "logs": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}
