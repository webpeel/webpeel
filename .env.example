# WebPeel Server Configuration
# Copy this to .env and update values

# Required: Secret for JWT signing (change this!)
JWT_SECRET=change-me-to-a-secure-random-string

# Server
NODE_ENV=production
PORT=3000

# Database (PostgreSQL)
DATABASE_URL=postgresql://webpeel:webpeel@db:5432/webpeel

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=*

# Stripe (optional — for billing/subscriptions)
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_PRO=price_...
# STRIPE_PRICE_MAX=price_...

# Sentry (optional — error tracking)
# SENTRY_DSN=https://<public-key>@o<org-id>.ingest.sentry.io/<project-id>
# SENTRY_ENVIRONMENT=production
# SENTRY_RELEASE=webpeel@0.8.1
# SENTRY_TRACES_SAMPLE_RATE=0.1

# Search APIs (optional — improves search-fallback for blocked sites)
# Google Custom Search: https://developers.google.com/custom-search/v1/introduction (100 queries/day free)
# GOOGLE_SEARCH_KEY=AIza...
# GOOGLE_SEARCH_CX=<custom-search-engine-id>
# Brave Search: https://api.search.brave.com/ (2,000 queries/month free)
# BRAVE_SEARCH_KEY=BSA...

# Anti-Bot Bypass: Cloudflare Worker Proxy (see worker/README.md)
# Deploy: cd worker && npx wrangler deploy
# WEBPEEL_CF_WORKER_URL=https://webpeel-proxy.<subdomain>.workers.dev
# WEBPEEL_CF_WORKER_TOKEN=<random-secret-for-auth>

# E-Commerce Domain APIs
# Best Buy Products API: https://developer.bestbuy.com/ (50K queries/day free)
# BESTBUY_API_KEY=<your-api-key>
